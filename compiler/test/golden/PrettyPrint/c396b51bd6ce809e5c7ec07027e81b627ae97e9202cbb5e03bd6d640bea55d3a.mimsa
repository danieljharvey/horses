let pred a =
  a == "o";

let fn s =
  match s with 
      a ++ as -> 
        (let rest =
          fn as;

        if (pred a)
        then
          a ++ rest
        else
          rest)
    | _ -> 
        "";

fn "woo"