type Array a 
  = Empty
  | Item a
         Array a;

let reduceA f b as =
  match as with 
      (Empty) -> 
        b
    | (Item a rest) -> 
        (reduceA f (f b a) rest);

reduceA (\a -> \b -> a + b) 0 Empty