let filter pred =
  let fn s =
    match s with 
        a ++ as -> 
          (let rest =
            fn as;

          if (pred a)
          then
            a ++ rest
          else
            rest)
      | _ -> 
          "";

  \str -> fn str;

filter (\a -> a == "o") "woo"