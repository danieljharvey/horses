let fn s =
  match s with 
      a ++ as -> 
        (let rest =
          fn as;

        if (a == "o")
        then
          a ++ rest
        else
          rest)
    | _ -> 
        "";

fn "woo"